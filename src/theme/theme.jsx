import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#0477BF',
      },
      secondary: {
        main: '#F285CC',
      },
      background: {
        paper: '#F21F0C',
      },
    }});